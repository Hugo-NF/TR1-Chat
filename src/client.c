// Project includes
#include "client.h"
#include "console.h"
#include "settings.h"

int main(int argc, char **argv) {

//    msg send, rcv;
//
//    int queue_id = msgget(NETWORK_KEY, 0x1FF);
//    if(queue_id < 0){
//        error("Could not connect to server\n");
//        exit(1);
//    }
//    msgrcv(queue_id, &rcv, sizeof(msg), 2, 0);
//
//    message("Client PID: %d\nMsg addr: %d\nCode: %d\n", getpid(), rcv.mtype, rcv.opcode);
//
//    send.mtype = 1;
//    send.opcode = -31;
//    msgsnd(queue_id, &send, sizeof(msg), 0);

    return 0;

}
